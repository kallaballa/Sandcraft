/*
 * Selection.cpp
 *
 *  Created on: Oct 8, 2021
 *      Author: elchaschab
 */

#include "Selection.hpp"

namespace sandcraft {

void Selection::draw() {
//		Config& cfg = Config::getInstance();
//		for (int i = cfg.btn_count_; i--;) {
//			ButtonInfo r = (*buttons_)[i];
//
//			SDL_Rect top = { r.rect.x, r.rect.y, Uint16(r.rect.w + 1), 1 };
//			SDL_Rect bottom = { r.rect.x, Sint16(r.rect.y + r.rect.h), Uint16(
//					r.rect.w + 1), 1 };
//			SDL_Rect left = { r.rect.x, r.rect.y, 1, Uint16(r.rect.h + 1) };
//			SDL_Rect right = { Sint16(r.rect.x + r.rect.w), r.rect.y, 1, Uint16(
//					r.rect.h + 1) };
//
//			if (particles_->currentParticleType_ == r.particleType) {
//				SDL_FillRect(sdl_->screen_, &top,
//						SDL_MapRGB(sdl_->screen_->format, 255, 0, 255));
//				SDL_FillRect(sdl_->screen_, &bottom,
//						SDL_MapRGB(sdl_->screen_->format, 255, 0, 255));
//				SDL_FillRect(sdl_->screen_, &left,
//						SDL_MapRGB(sdl_->screen_->format, 255, 0, 255));
//				SDL_FillRect(sdl_->screen_, &right,
//						SDL_MapRGB(sdl_->screen_->format, 255, 0, 255));
//			} else {
//				SDL_FillRect(sdl_->screen_, &top,
//						SDL_MapRGB(sdl_->screen_->format, 255, 255, 255));
//				SDL_FillRect(sdl_->screen_, &bottom,
//						SDL_MapRGB(sdl_->screen_->format, 255, 255, 255));
//				SDL_FillRect(sdl_->screen_, &left,
//						SDL_MapRGB(sdl_->screen_->format, 255, 255, 255));
//				SDL_FillRect(sdl_->screen_, &right,
//						SDL_MapRGB(sdl_->screen_->format, 255, 255, 255));
//			}
//		}
	}

} /* namespace sandcraft */
